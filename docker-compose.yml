services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: lkfd
      POSTGRES_PASSWORD: lkfd
      POSTGRES_DB: lkfd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
